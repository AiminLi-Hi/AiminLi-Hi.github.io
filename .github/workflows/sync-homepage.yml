name: Sync Scholar data

on:
  schedule:
    # 05:17 in Europe/Istanbul, expressed in UTC.
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: homepage-data-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Sync Google Scholar data
        run: npm run sync:scholar
        env:
          SCHOLAR_USER_ID: nyl1-EMAAAAJ

      - name: Validate app
        run: |
          npm run lint
          npm run build
        env:
          VITE_VISITOR_STATS_ENDPOINT: https://aimin-homepage-visitors-api.pages.dev

      - name: Commit synchronized source data
        id: commit-data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/homepage-sync-data.js
          if git diff --cached --quiet; then
            echo "No synchronized Scholar data changes."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "Sync homepage Scholar data"
            git push origin HEAD:main
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy GitHub Pages
        if: steps.commit-data.outputs.changed == 'true'
        run: npm run deploy:pages
        env:
          VITE_VISITOR_STATS_ENDPOINT: https://aimin-homepage-visitors-api.pages.dev
          GIT_AUTHOR_NAME: github-actions-bot
          GIT_AUTHOR_EMAIL: github-actions-bot@users.noreply.github.com
